name: Deploy to Production

on: [push]

jobs:
  build:
    name: Build
    runs-on: [self-hosted, win]
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to server
        run: |
          cd ~/
          ls -al
